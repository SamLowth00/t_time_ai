// Shared camera framing for both the static tee view and the fly-through.
export const FLY_TILT = 65; // degrees from nadir (0 = straight down, 90 = horizontal)
export const FLY_RANGE = 45; // metres from the camera to the look-at center
